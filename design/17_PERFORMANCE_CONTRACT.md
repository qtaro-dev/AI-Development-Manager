# P0-015 性能ベースライン契約

版: 1.0-p0-015
状態: 測定完了、現方式の正式採用は保留
基準日: 2026-08-02

## 測定条件

- SDK: .NET 10.0.302（ルート `global.json` 固定）
- Runtime: .NET 10.0.10 / RID: win-x64
- 文書数: 10,000、同時利用モデル: 人5、AIクライアント2
- サイズ分布: 1–16 KiB 7,000件、16–256 KiB 2,500件、256 KiB–2 MiB 500件
- 乱数シード: 15015、ウォームアップ3回除外、測定20回、p95は最近順位法
- 出力: `%TEMP%\AI-Development-Manager\poc\P0-015\<run-id>\result.json`

## 実測

| 指標 | 暫定基準 | p50 | p95 | 判定 |
|---|---:|---:|---:|---|
| 初回走査 | 3,000 ms以内 | 4,660.3 ms | 5,141.1 ms | 未達 |
| 候補一覧 | 3,000 ms以内 | 0.4 ms | 0.5 ms | 合格 |
| 索引済み検索 | 500 ms以内 | 875.7 ms | 945.2 ms | 未達 |

索引作成は28,124.4ms、コーパスは685,673,224 bytes、ピークワーキングセットは1,905,491,968 bytes、出力ディスク使用量は2,129,558,280 bytesだった。7クライアント相当70操作でETag競合2件を拒否し、索引整合性を維持した。

走査と検索の未達はPhase 1改善候補（増分化、索引投入方式、Tokenizer・容量の再評価、メモリ監視）へ分離する。正確性を犠牲にした対策は採用しない。PoC成果物は製品コードへ昇格させない。

P0-015は再実施せず、この値を改善前ベースラインとして保持する。現方式の正式採用は保留し、P0-024（走査）、P0-025（FTS最適化）、P0-026（SQLite依存安全性）を追加PoC候補としてP0-023で最終判断する。
