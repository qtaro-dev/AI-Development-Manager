# DevTicketManager互換・移行境界契約（P0-022）

## 状態

承認済み匿名化コピーによる互換検証を完了。`poc/devticketmanager-compat`の読み取り専用ハーネスで9項目すべて合格した。正式な移行仕様と`.adm-meta`スキーマ判断はP0-023へ分離する。

## 入力と安全境界

- `--input`にはユーザー提供の隔離済み匿名化コピーだけを指定する。
- 入力ディレクトリを再帰的に読み、Markdown以外は読み飛ばす。個別読取エラーは記録し、全体走査を停止しない。
- UTF-8 BOM、UTF-8、Windows code page 932（画面表記はShift_JIS）を判定候補とする。
- PoCは入力へ書き込まず、Front Matter追加、改名、移動、`.adm-meta`作成を行わない。
- 前後のツリーSHA-256が一致することを確認する。

## 互換マッピング（実データ確認前の項目定義）

| 機能 | 判定 | 実データで確認する証拠 |
|---|---|---|
| Markdown一覧・本文 | 確認済み | 7 Markdown、見出し・本文・UTF-8を読み取り |
| 作成・更新日時 | 確認済み | ファイル更新日時を取得、意味の正式確定は保留 |
| 名前・日時ソート | 確認済み | 名前・日時ソート処理を実行 |
| 検索 | 確認済み | 検索処理を実行 |
| 確認状態 | 条件付き確認 | `.adm-meta`写像候補を確認、正式スキーマはP0-023 |
| 添付関係 | 確認済み | 9件の添付候補を抽出 |
| プロジェクト切替 | 条件付き確認 | プロジェクト概要とticket構造を確認、正式移行仕様はP0-023 |

## `.adm-meta`写像方針

確認状態などの補助情報は、元Markdownを変更しない`.adm-meta`オーバーレイへの写像候補として記録する。正式スキーマ、移行操作、既存値の優先順位は実データ確認とP0-023の設計ゲートを経て確定する。PoCは写像案を提示するだけで、変換・一括移行を実行しない。

## P0-022再検証結果

- 入力: `D:\Dev\AI-Development-Manager-P0-022\input-anonymized`
- 7 Markdownファイル、個別読取エラー0件、添付候補9件。
- 入力前後のツリーSHA-256一致。
- SDK 10.0.302、Runtime 10.0.10。
- 結果JSON: `%TEMP%\AI-Development-Manager\poc\P0-022\20260802-133847-161312a7\result.json`
- 判定: `PASSED`。P0-022正式完了。

## 未解決事項

- 実データの提供待ち。サンプルが運用全体を代表するかをユーザーが確認する。
- ファイル作成日時のコピーによる意味変化を実環境で確認する。
- 実データに秘密情報が残っていないことを提供前に確認する。
